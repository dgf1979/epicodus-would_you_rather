console.log("update.js.erb");
$(document).ready(function() {
  $("form input:text").val("0");
  var renderDiv = "#vote-display-" + "<%= @choice.id %>";
  console.log("render votes display to div with id: " + renderDiv)
  $(renderDiv).html("<%= j (render 'votes', choice: @choice) %>");
});
